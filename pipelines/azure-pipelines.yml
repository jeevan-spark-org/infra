name: infra-$(Date:yyyyMMdd)$(Rev:.r)

trigger:
  branches:
    include:
      - main
  paths:
    include:
      - pipelines/**
      - templates/**
      - scripts/**

pr:
  branches:
    include:
      - main
  paths:
    include:
      - pipelines/**
      - templates/**
      - scripts/**

resources:
  repositories:
    - repository: pipeline_common
      type: github
      endpoint: jeevan-spark-org
      name: jeevan-spark-org/infra-pipeline-common

variables:
  - template: pipelines/variables/common.yml@pipeline_common

extends:
  template: pipelines/extends/infra-deploy.yml@pipeline_common
  parameters:
    azureServiceConnection: $(azureServiceConnection)
    location: '$(location)'
    deploymentTemplates:
      - name: network
        templateFile: infra/templates/network/network.bicep
        parameterFile: infra/templates/network/network.bicepparam
      - name: loganalytics
        templateFile: infra/templates/loganalytics/loganalytics.bicep
        parameterFile: infra/templates/loganalytics/loganalytics.bicepparam
      - name: acr
        templateFile: infra/templates/acr/acr.bicep
        parameterFile: infra/templates/acr/acr.bicepparam
      - name: aks
        templateFile: infra/templates/aks/aks.bicep
        parameterFile: infra/templates/aks/aks.bicepparam
